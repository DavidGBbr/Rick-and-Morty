import React from "react";
import { Container, Loader } from "./style";

const LoadComponent = () => {
  return (
    <Container>
      <Loader></Loader>
    </Container>
  );
};

export default LoadComponent;
